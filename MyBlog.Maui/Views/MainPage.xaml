<?xml version="1.0" encoding="UTF-8"?>

<ContentPage
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:vm="clr-namespace:MyBlog.Maui.ViewModels"
    x:Name="This"
    x:Class="MyBlog.Maui.Views.MainPage"
    Title="My Blog Posts">

    <RefreshView
        IsRefreshing="{Binding IsRefreshing}"
        Command="{Binding RefreshCommand}">

        <StackLayout
            Padding="16"
            Spacing="8">

            <!-- Título de la página -->
            <!-- <Label  -->
            <!--     Text="Welcome to MyBlog" -->
            <!--     Style="{StaticResource TitleLabelStyle}"  -->
            <!--     Margin="0,0,0,10" /> -->

            <!-- Lista de Posts -->
            <CollectionView
                ItemsSource="{Binding Posts}"
                SelectionMode="None">

                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <!-- Aplica el estilo de Frame -->
                        <Frame Style="{StaticResource FramePostStyle}">
                            <StackLayout>
                                <Label
                                    Text="{Binding Title}"
                                    Style="{StaticResource TitleLabelStyle}"
                                    FontSize="18" />

                                <BoxView
                                    HeightRequest="1"
                                    Color="LightGray"
                                    Margin="0,0,0,4" />

                                <Label
                                    Text="{Binding Body}"
                                    Style="{StaticResource BodyLabelStyle}" />
                                <StackLayout.GestureRecognizers>
                                    <TapGestureRecognizer 
                                        Command="{Binding Source={RelativeSource AncestorType={x:Type vm:MainViewModel}}, Path=OpenPostCommand}" 
                                        CommandParameter="{Binding .}" />
                                </StackLayout.GestureRecognizers>
                            </StackLayout>
                           
                        </Frame>
                    
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </StackLayout>
    </RefreshView>
</ContentPage>